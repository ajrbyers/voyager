{# app-shell — sidebar + main content grid.
   Use as a layout wrapper. The caller provides the sidebar markup
   (typically the sidebar-nav macro) and the page content. #}
{% macro app_shell() %}
<div class="app-shell">
  {{ caller() }}
</div>
{% endmacro %}
